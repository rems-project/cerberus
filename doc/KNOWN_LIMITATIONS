This file the features of C11 currently not supported for specific reasons.



* cast operators inside `integer constant expressions' (see §6.6#6).
  REASON:
    to check there validity we need typing informations during desugaring




* variables in `integer constant expression', the STD seems to allow it (by not saying
  anything about it). But both gcc and clang don't like them. Except for const-qualified
  global variables in the case of Clang.
fun main() : integer :=
    let (x: integer, y: integer) = (1, 2) in
    let (a: integer, b: integer, c: integer) = (3, 4, 5) in  
    let (p: (integer, integer), q: integer) = ((6, 7), 8)  
    in 
      if (a =  3) then
          undef(<<UB045a_division_by_zero>>) 
      else 0

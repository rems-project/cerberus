return code: 0
proc main (): eff loaded integer :=
  let strong (x: loaded integer, y: loaded integer, z: loaded integer) =
    let strong (x: loaded integer) =
      bound(pure(Unit)) ;
      pure((conv_loaded_int('signed int', Specified(10)))) in
    let strong (y: loaded integer) =
      bound(pure(Unit)) ;
      pure((conv_loaded_int('signed int', Specified(20)))) in
    let strong (z: loaded integer) =
      let strong a_511: loaded integer =
        bound(
          let weak (a_512: loaded integer, a_513: loaded integer) =
            unseq(
              let weak _: unit = pure(Unit) in
              pure(x)
            ,
              let weak _: unit = pure(Unit) in
              pure(y)
            ) in
          pure(
            case (a_512, a_513) of
              | (Specified(a_514: integer), Specified(a_515: integer)) =>
                  Specified(catch_exceptional_condition_add('signed int', __conv_int__('signed int', a_514), __conv_int__('signed int', a_515)))
              | _: (loaded integer,loaded integer) =>
                  undef(<<UB036_exceptional_condition>>)
            end
          )
        ) in
      pure((conv_loaded_int('signed int', a_511))) in
    let strong a_520: loaded integer =
      bound(
        let weak _: unit = pure(Unit) in
        pure(z)
      ) in
    pure(Unit) ;
    pure(Unit) ;
    pure(Unit) ;
    run ret_508(conv_loaded_int('signed int', a_520)) ;
    pure(Unit) ;
    pure(Unit) ;
    pure(Unit) ;
    pure((x, y, z)) in
  save ret_508: loaded integer (a_521: loaded integer:= Specified(0)) in
    pure(a_521)

Defined {value: "Specified(30)", stdout: "", stderr: "", blocked: "false"}

return code: 0
proc main (): eff loaded integer :=
  let strong (x: loaded integer, a: loaded integer) =
    let strong (x: loaded integer, a: loaded integer) =
      let strong (a: loaded integer, a_507: loaded integer) =
        bound(
          let weak (a: loaded integer) =
            let weak _: unit = pure(Unit) in
            let weak (a: loaded integer) = pure((Specified(42))) in
            pure((a)) in
          pure((a, a))
        ) in
      pure((conv_loaded_int('signed int', a_507), a)) in
    let strong a_512: loaded integer =
      bound(
        let weak _: unit = pure(Unit) in
        pure(x)
      ) in
    pure(Unit) ;
    run ret_506(conv_loaded_int('signed int', a_512)) ;
    pure(Unit) ;
    pure(Unit) ;
    pure((x, a)) in
  save ret_506: loaded integer (a_513: loaded integer:= Specified(0)) in
    pure(a_513)

Defined {value: "Specified(42)", stdout: "", stderr: "", blocked: "false"}

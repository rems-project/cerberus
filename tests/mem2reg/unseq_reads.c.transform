return code: 0
proc main (): eff loaded integer :=
  let strong (x: loaded integer) =
    let strong (x: loaded integer) =
      let strong (x: loaded integer) =
        bound(pure(Unit)) ;
        pure((conv_loaded_int('signed int', Specified(7)))) in
      let strong a_434: loaded integer =
        bound(
          let weak (a_427: loaded integer, a_428: loaded integer) =
            unseq(
              let weak _: unit = pure(Unit) in
              pure(x)
            ,
              let weak _: unit = pure(Unit) in
              pure(x)
            ) in
          pure(
            case (a_427, a_428) of
              | (Specified(a_429: integer), Specified(a_430: integer)) =>
                  Specified(catch_exceptional_condition_add('signed int', __conv_int__('signed int', a_429), __conv_int__('signed int', a_430)))
              | _: (loaded integer,loaded integer) =>
                  undef(<<UB036_exceptional_condition>>)
            end
          )
        ) in
      pure(Unit) ;
      run ret_425(conv_loaded_int('signed int', a_434)) ;
      pure(Unit) ;
      pure((x)) in
    pure((x)) in
  save ret_425: loaded integer (a_435: loaded integer:= Specified(0)) in
    pure(a_435)

Defined {value: "Specified(14)", stdout: "", stderr: "", blocked: "false"}

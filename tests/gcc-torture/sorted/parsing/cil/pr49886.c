#include "cerberus.h"
/* Generated by CIL v. 1.7.3 */
/* print_CIL_Input is false */

struct PMC {
   unsigned int flags ;
};
struct Pcc_cell {
   struct PMC *p ;
   long bla ;
   long type ;
};
typedef struct Pcc_cell Pcc_cell;
int gi  ;
int cond  ;
extern void abort() ;
void ( __attribute__((__noinline__)) never_ever)(int interp , struct PMC *pmc )  __attribute__((__noclone__)) ;
void ( __attribute__((__noinline__)) never_ever)(int interp , struct PMC *pmc )  __attribute__((__noclone__)) ;
void ( __attribute__((__noinline__)) never_ever)(int interp , struct PMC *pmc ) 
{ 


  {
  abort();
  return;
}
}
static void ( __attribute__((__nonnull__(1))) mark_cell)(int *interp , Pcc_cell *c ) ;
static void ( __attribute__((__nonnull__(1))) mark_cell)(int *interp , Pcc_cell *c ) 
{ 


  {
  if (! cond) {
    return;
  }
  if (((c && c->type == 4L) && c->p) && ! ((c->p)->flags & (unsigned int )(1 << 18))) {
    never_ever(gi + 1, c->p);
  }
  if (((c && c->type == 4L) && c->p) && ! ((c->p)->flags & (unsigned int )(1 << 17))) {
    never_ever(gi + 2, c->p);
  }
  if (((c && c->type == 4L) && c->p) && ! ((c->p)->flags & (unsigned int )(1 << 16))) {
    never_ever(gi + 3, c->p);
  }
  if (((c && c->type == 4L) && c->p) && ! ((c->p)->flags & (unsigned int )(1 << 15))) {
    never_ever(gi + 4, c->p);
  }
  if (((c && c->type == 4L) && c->p) && ! ((c->p)->flags & (unsigned int )(1 << 14))) {
    never_ever(gi + 5, c->p);
  }
  if (((c && c->type == 4L) && c->p) && ! ((c->p)->flags & (unsigned int )(1 << 13))) {
    never_ever(gi + 6, c->p);
  }
  if (((c && c->type == 4L) && c->p) && ! ((c->p)->flags & (unsigned int )(1 << 12))) {
    never_ever(gi + 7, c->p);
  }
  if (((c && c->type == 4L) && c->p) && ! ((c->p)->flags & (unsigned int )(1 << 11))) {
    never_ever(gi + 8, c->p);
  }
  if (((c && c->type == 4L) && c->p) && ! ((c->p)->flags & (unsigned int )(1 << 10))) {
    never_ever(gi + 9, c->p);
  }
  return;
}
}
static void foo(int *interp , Pcc_cell *c ) 
{ 


  {
  mark_cell(interp, c);
  return;
}
}
static struct Pcc_cell * __attribute__((__noclone__)) ( __attribute__((__noinline__)) getnull)(void) 
{ 


  {
  return ((struct Pcc_cell *)0);
}
}
int main(void) 
{ 
  int i ;
  struct Pcc_cell * __attribute__((__noclone__)) tmp ;

  {
  cond = 1;
  i = 0;
  while (i < 100) {
    tmp = getnull();
    foo(& gi, tmp);
    i ++;
  }
  return (0);
}
}
void bar_1(int *interp , Pcc_cell *c ) 
{ 


  {
  (c->bla) ++;
  mark_cell(interp, c);
  return;
}
}
void bar_2(int *interp , Pcc_cell *c ) 
{ 


  {
  c->bla += 2L;
  mark_cell(interp, c);
  return;
}
}

int main() {
    int toplevel, i;
    if (1) {
        int nested;
        1;
    }

    while(1){2;}

    // TODO: support declaring variables here?
    for(i = 0; i < toplevel; i += 1) {
        3;
    }

    // TODO: support || and &&
    // if (1 || 2 && 3) {}
}

(* Useful for "explicit" overloading of operators. *)

module Operators1 = struct
  let (>>) b1 b2 = b1 && b2
end

module Operators2 = struct
  let (>>) b1 b2 = b1 || b2
end

let f1 b1 b2 =
  let open Operators1 in
  b1 >> b2

let f2 b1 b2 =
  let open Operators2 in
  b1 >> b2

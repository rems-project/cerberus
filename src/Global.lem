type ('a, 'b) map = ('a, 'b) Pmap.map
type 'a multiset = 'a Multiset.t

let (-|) f g x = f (g x)
let (|-) g f x = f (g x)
let (>>) x f = f x

let compare_num i1 i2 =
  if i1 = i2 then
    Ord.EQ
  else if i1 < i2 then
    Ord.LT
  else
    Ord.GT
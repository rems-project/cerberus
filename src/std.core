--  in_range :: ctype -> value -> boolean
fun in_range ty v =
  min{ty} <= v \/ v <= max{ty}

--  overflow :: ctype -> value -> value
fun overflow ty v =
  if in_range ty v then v
                   else undef

--  

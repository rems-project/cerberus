type ('a, 'b) map = ('a, 'b) Pmap`map
type 'a multiset = 'a multiset`t

let (-|) f g x = f (g x)
let (|-) g f x = f (g x)
let (>>) x f = f x

type 'a error =
  | UNDEFINED of 'a
  | INVALID of 'a
  | BUG of 'a